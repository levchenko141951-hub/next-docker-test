name: Deploy Frontend
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: docker buildx build --platform linux/amd64 -t ghcr.io/${{ secrets.USER }}/next-app:${{ github.sha }} .

      - name: Login
        run: echo ${{ secrets.PASS }} | docker login ghcr.io -u ${{ secrets.USER }} --password-stdin

      - name: Push
        run: docker push ghcr.io/${{ secrets.USER }}/next-app:${{ github.sha }}

      - name: Deploy
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} "
            docker login ghcr.io -u ${{ secrets.USER }} -p ${{ secrets.PASS }}
            docker rm -f app || true && \
            docker pull ghcr.io/${{ secrets.USER }}/next-app:${{ github.sha }} &&
            docker run -d -p 80:80 --name next-app ghcr.io/${{ secrets.USER }}/next-app:${{ github.sha }}
          "

#      - name: Build Docker image
#        run: docker build --no-cache -t next-app-${{ github.sha }} .
#
#      - name: Save Docker image to tar
#        run: docker save next-app-${{ github.sha }} > next-app-${{ github.sha }}.tar
#
#      - name: Upload + deploy
#        run: |
#          scp next-app-${{ github.sha }}.tar ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:/tmp
#
#          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
#            docker stop next-app || true
#            docker rm next-app || true
#            docker load < /tmp/next-app-${{ github.sha }}.tar
#            docker run -d --name next-app -p 80:80 --restart unless-stopped next-app-${{ github.sha }}
#          EOF
